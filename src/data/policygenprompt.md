from the definition of the policy table, generate a 50-row csv file, surrounding all non-integers with double quotes and store it in data\policy.csv. and put the code that generated it into a file calleddata\ policygenerator.py . For each policy, choose a postal code from the list of postal codes, and use that entire row of information when populating city, stateprovince, postalcode, latitude and longitude .

from the definition of the superfundsite table, generate a 50-row csv file, surrounding all non-integers with double quotes and store it in data\superfundsites.csv. and put the code that generated it into a file called data\ superfundsitesgenerator.py.  For each row, choose a postal code from the list of postal codes, and use that entire row of information when populating city, stateprovince, postalcode, latitude and longitude .
